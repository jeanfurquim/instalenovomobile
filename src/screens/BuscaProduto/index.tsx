import { StyleSheet, Text, View } from "react-native";
import React from "react";
import BottomTabs from "../../components/BottomTabs";

const BuscaProduto = () => {
  return (
    <>
      <View>
        <Text>BuscaProduto</Text>
      </View>
      <BottomTabs />
    </>
  );
};

export default BuscaProduto;

const styles = StyleSheet.create({});
